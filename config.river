declare "db" {
    argument "names" {}
    prometheus.exporter.postgres "db" {
        data_source_names = names
    }

    export "targets" {
        value = prometheus.exporter.postgres.db.targets
    }
}
